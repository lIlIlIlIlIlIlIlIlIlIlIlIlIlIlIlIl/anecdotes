#!/usr/bin/env bash
FILE="ANECDOTES.md"
TITLE="# Anecdotes 📖"

COUNT=$(grep -c "^- " "$FILE")

awk -v title="$TITLE" -v count="$COUNT" '
  NR == 1 { print title " (" count ")"; next }
{ print }
' "$FILE" > "$FILE.tmp" && mv "$FILE.tmp" "$FILE"

git add "$FILE"